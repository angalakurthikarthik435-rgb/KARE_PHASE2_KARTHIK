import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner obj = new Scanner(System.in);
        int n = obj.nextInt();
        int temp = n,d,rev=0;
        while(n!=0) {
            d=n%10;
            rev = rev*10+d;
            n=n/10;
        }
        if(temp==rev)
        System.out.print("Pallindrome Number");
        else
        System.out.print("Not a Pallindrome");
    }
}

//write a java program to reverse the number without using rightside formation
import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner obj = new Scanner(System.in);
        int n = obj.nextInt();
        int d;
        while(n!=0) {
            d=n%10;
            System.out.print(d);
            n=n/10;
        }
        
    }
}

//write a java program to reverse the number with using rightside formation
import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner obj = new Scanner(System.in);
        int n = obj.nextInt();
        int d,ans=0;
        while(n!=0) {
            d=n%10;
            ans = ans*10+d;
            n=n/10;
            System.out.print("Reverse Number :"+ans);
        }
        
    }
}

//write a java program on even digits in the given numbers
import java.util.*;
class Main {
    public static void main(String[] args) {
        Scanner obj = new Scanner(System.in);
        int n = obj.nextInt();
        int d,ed=0,od=0;
        while(n!=0) {
            d=n%10;
            if(d%2==0)
            ed++;
            else
            od++;
            n=n/10;
            System.out.println("odd digit count: "+od);
            System.out.println("even digit count: "+ed);
        }
        
    }
}
